---
- name: Symfony with Vagrant-Ansible
  hosts: webservers
  sudo: yes
  user: vagrant

  vars_files:
    - vars.yml

  roles:
    - common
    - git
    - php-fpm
    - composer
    - nginx
    - mysql